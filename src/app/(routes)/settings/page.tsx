import SpaceContainer from "@/app/components/space-container/SpaceContainer";

const Settings = () => {
    return (
        <SpaceContainer>
            Settings
        </SpaceContainer>
    );
};

export default Settings;
